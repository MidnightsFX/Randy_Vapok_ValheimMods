namespace EpicLoot.MagicItemEffects.Shards
{
    // Boss shard signature effects — STUBS ONLY (no behavior), one per Valheim boss. Boss-tier power;
    // inert until implemented.

    public static class ShockingCharge { }   // Eikthyr  — TODO: build charges on attack; release lightning at max
    public static class ForestsAid { }       // The Elder — TODO: summon ensnaring roots that root/slow nearby enemies
    public static class PoisonCoating { }    // Bonemass  — TODO: attacks apply stacking poison; wearer gains poison resist
    public static class IcyRetribution { }   // Moder     — TODO: on hit taken, chance to emit a frost nova
    public static class MeteorSummoner { }   // Yagluth   — TODO: chance on hit to call a fiery meteor (AOE fire)
    public static class EitrSiphon { }       // The Queen — TODO: hits/kills siphon eitr and boost eitr regen
    public static class LastFire { }         // Fader     — TODO: at low health, erupt in a fire nova + brief fire immunity
}
